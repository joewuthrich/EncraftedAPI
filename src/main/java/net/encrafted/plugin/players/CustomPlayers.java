package net.encrafted.plugin.players;

import org.bukkit.event.Listener;

import java.util.UUID;

public class CustomPlayers implements Listener {
    /**
     * Get a custom player by UUID
     * @param uuid The UUID of the player
     * @return The custom player if they exist, or null if else
     */
    public static CustomPlayer getPlayer(UUID uuid) {
        return null;
    }
}
